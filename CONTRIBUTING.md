# Contributing

Keep the shared room protocol and daemon API behavior compatible across Windows, Linux, and Android. Run the desktop core tests and the platform build you changed. Never commit credentials, room databases, signing keys, generated binaries, or private build paths.

A project license and formal contribution policy have not yet been selected.
